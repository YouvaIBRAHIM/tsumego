export interface IProfile {
    username: string;
    firstName: string;
    lastName: string;
    email: string;
}

export interface IPasswords {
    currentPassword: string;
    newPassword: string;
    confirmPassword: string;
}
